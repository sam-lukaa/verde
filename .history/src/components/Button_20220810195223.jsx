export const Button = ({children}) => {
  return (
    <div>           
      <button className='bg-blue-600 text-white p-[] lg:px-[1rem] lg:py-[.7rem] font-[500] rounded-[] lg:rounded-[8px]'>{children}</button>
    </div>
  )
}